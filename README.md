# TV-Shows

# SETUP

1. Install Node.js

2. Open terminal

3. Run cd server/ run cd react-frontend

4. Run npm install in each directory

5. In server directory run tsc and then node server.js/ for tests run npm test

6. In react-frontend director run npm start